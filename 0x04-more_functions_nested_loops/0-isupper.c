#include "main.h"
/**
 * _isupper -prog cheks for uppercase character
 * @c : Variable text
 * Return : Always 0.
 */
int _issuper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
} 
